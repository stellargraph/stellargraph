---
version: "3.4"
services:
  stellargraph:
    build:
      dockerfile: docker/stellargraph/Dockerfile
      context: .
    environment:
      - DOCKER_CLIENT_TIMEOUT=180
      - COMPOSE_HTTP_TIMEOUT=180
